export const incrementCounter = () => ({
    type: 'INCREMENT', // corrected to match the reducer
  });
  
  export const decrementCounter = () => ({
    type: 'DECREMENT', // corrected to match the reducer
  });
  